extends: default

rules:
  braces:
    forbid: non-empty
  document-start:
    present: false # Don't require `---` at the start of the file
  indentation:
    spaces: 2
    indent-sequences: false
  key-ordering: enable
